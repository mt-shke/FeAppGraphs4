describe("add", () => {
   it("should compare the sum of expectedResult to result", () => {
      const expectedResult = 2 + 2;

      const result = 4;

      // Assurez-vous que le résultat de la fonction correspond aux attentes
      expect(result).toEqual(expectedResult);
   });
});
